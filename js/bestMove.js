function bestMove(state, depth, player){
    var best;

    if(player == AI){
        best = [-10,-10,-1000];
    }
    else{
        best = [-10,-10,+1000];
    }

    if(depth ==0 || allOver(cell)){
        
    }
}